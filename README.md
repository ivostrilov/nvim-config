# nvim-config

### Get started

```bash
mv ~/.config/nvim ~/.config/nvim.backup
git clone https://github.com/ivostrilov/nvim-config.git ~/.config/nvim
```

#### Dependencies

Package manager 'Lazy' requires:
 - luarocks - pkg manager for lua.

Plugin 'fzf-lua' requires:
 - fzf, ripgrep - cmd utilities.

***
 
### List of plugins

Check `lua/plugins`.

***

### Some hotkeys

TODO!

#### LSP

- K - get float window with info.

Common prefix `<leader>l` with:
- f - format current file.

***
